spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 500MB
      max-request-size: 515MB
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
file:
  dir: C:/test/
  security:
    user:
      name: user
      password: test


